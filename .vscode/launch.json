{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "program": "${workspaceFolder}/src/server.js",
      "preLaunchTask": "tsc: build - tsconfig.json",
      "runtimeArgs": ["-r", "module-alias/register", "-r", "dotenv/config"],
      "outFiles": ["${workspaceFolder}/build/server/**/*.js"]
    }
  ]
}
